import { BrowserFile, NodeFile } from '../request/types';
export declare type ReactNativeAsset = {
    name?: string;
    type?: string;
    uri: string;
};
export declare type FileTransformer = (file: NodeFile | BrowserFile, name: string) => NodeFile | BrowserFile | ReactNativeAsset;
