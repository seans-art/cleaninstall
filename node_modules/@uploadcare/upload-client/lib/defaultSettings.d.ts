import { DefaultSettings } from './types';
declare const defaultSettings: DefaultSettings;
declare const defaultContentType = "application/octet-stream";
declare const defaultFilename = "original";
export { defaultFilename, defaultContentType, defaultSettings };
export default defaultSettings;
